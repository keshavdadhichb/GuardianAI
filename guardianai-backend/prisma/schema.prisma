generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Define the model for our alert events
model AlertEvent {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  type      String
  verdict   String
}